// Tutor's student detail view — same as manager's, role detection happens inside.
// The component reads useAuth() and adjusts buttons/permissions based on role.
export { default } from '../manager/StudentDetail'
